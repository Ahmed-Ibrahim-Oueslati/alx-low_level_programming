#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point of the program.
 *
 * Description: Prints a string to the console.
 *
 * Return: Always 0 (success).
 */
int main(void)
{
int i=48,k=97 ;

        while (i<58)
        {
        putchar(i);
        i++;
        }
	  while (k<103)
        {
        putchar(k);
        k++;
        }
        putchar(10);
return (0);
}
