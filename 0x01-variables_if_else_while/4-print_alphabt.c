#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point of the program.
 *
 * Description: Prints a string to the console.
 *
 * Return: Always 0 (success).
 */
int main(void)
{
int i=97 ;              
        while (i<123)
	{
	if (i!=113 && i!=101)
	{
	putchar(i);
	
	}
	i++;
	}
	putchar(10);	
return (0);   
}
