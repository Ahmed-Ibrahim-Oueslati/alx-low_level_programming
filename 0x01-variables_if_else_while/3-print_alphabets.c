#include <stdlib.h>
#include <stdio.h>
/**
 * main - Entry point of the program.
 *
 * Description: Prints a string to the console.
 *
 * Return: Always 0 (success).
 */
int main(void)
{
int i=97,k=65 ;

        while (i<123)
        {
        putchar(i);
        i++;
        }
	  while (k<90)
        {
        putchar(k);
        k++;
        }
        putchar(10);
return (0);
}
