#include <stdlib.h>
#include "main.h"
#include <stdio.h>
/**
 * add - Entry point of the program.
 *
 * Description: Prints a string to the console.
 *
 * Return: Always 0 (success).
 */
 int add(int c, int b)
{
int j;
j = c + b; 
return (j);
}
